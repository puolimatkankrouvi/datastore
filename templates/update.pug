extends base.pug

block content
  h1 Edit
  form(method='POST', action='/edit/' + id +'/')
    div.form-group
      input.form-control(type='text',name='edited_text', value=orginal_text)
    div.form-group
      input.form-control(type='hidden',name='id',value=id)
    div.form-group
      button(type='submit', class='btn btn-default') Submit
